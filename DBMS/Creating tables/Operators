CREATE TABLE Operators (
    operator_name VARCHAR(100) PRIMARY KEY NOT NULL,
    base_of_operation VARCHAR(100) NOT NULL,
    fleet_size INT NOT NULL,
    website VARCHAR(100),
    address VARCHAR(255),
    number_of_staff INT NOT NULL
);
